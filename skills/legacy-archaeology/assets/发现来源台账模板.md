# 发现来源台账 · 〔服务标识〕

> 这本台账记录**「我是怎么找的、找的边界在哪」**，不是「找到了什么」（那是对象覆盖台账）。
> 它的核心价值是诚实——把白盒扫描照不到的盲区**主动列出来**，而不是假装查全了。
> 复制本模板后逐处替换 `〔填空〕`；不适用的项显式写「本项目不适用」。

## 一、枚举来源（用了哪些白盒手段）

| 对象类型 | 枚举手段 | 命中范围 |
|---|---|---|
| HTTP 接口 | 〔填空，示例：`@*Mapping` 注解扫描〕 | 〔填空，示例：controller 包全覆盖〕 |
| RPC | 〔填空，示例：`@FeignClient` + `@DubboReference`〕 | 〔填空〕 |
| MQ 收发 | 〔填空，示例：`@RocketMQMessageListener` + `rocketMQTemplate.send*`〕 | 〔填空〕 |
| 表 | 〔填空，示例：MyBatis XML + `@Entity`〕 | 〔填空〕 |
| 定时任务 | 〔填空，示例：`@Scheduled` + Quartz JobDetail〕 | 〔填空〕 |
| 外部调用 | 〔填空，示例：`RestTemplate`/`WebClient` 调用点 grep〕 | 〔填空〕 |

## 二、已知未覆盖（白盒照不到的盲区，主动列出）

> 这一栏越诚实，知识库越可信。把「我知道我没查到的」写清楚。

- 〔填空，示例：反射式 RPC 泛化调用，无静态锚点〕
- 〔填空，示例：运行时动态拼接的 SQL（表名由变量决定）〕
- 〔填空，示例：topic 由配置中心下发的 MQ 发送点〕
- 〔填空 / 本项目不适用〕

## 三、外部真值源（运行时真值，做差集找盲区）

> 这里只记每个真值源的「获取 + 是否已按服务边界裁剪 + 是否已比对」状态；差出的盲区**逐项**落到下面第四节，
> 不在这里写「发现 N 个」的汇总。

| 真值源 | 获取状态 | 比对状态（差出项见四、） |
|---|---|---|
| 网关路由表 | 〔已获取 / 未获取〕 | 〔示例：已按本服务路由前缀裁剪并比对〕 |
| 注册中心服务列表 | 〔已获取 / 未获取〕 | 〔填空〕 |
| 生产 access log（URL 去重） | 〔已获取 / 未获取〕 | 〔填空〕 |
| MQ broker topic 列表 | 〔已获取 / 未获取〕 | 〔示例：按 consumer group 裁剪后比对〕 |
| DB `information_schema` 表清单 | 〔已获取 / 未获取〕 | 〔示例：未获取 → 数据库层整层标低置信〕 |
| 调度平台导出（XXL-JOB/crontab） | 〔已获取 / 未获取〕 | 〔填空〕 |

> **未获取的真值源 = 对应对象类型不给绿灯。** 在对象覆盖台账的「分类置信度声明」里把对应类型标
> 「仅白盒、未经运行时校验、可能漏列」。

## 四、外部差集盲区清单（逐项闭环，不许只写一句汇总）

> §5.3 第三步：差集差出来的每个盲区**逐项**登记于此，且每一项必须有归宿——
> 要么补查后补进对象覆盖台账，要么明列入残余风险。**禁止**只写「发现 N 个漏掉的接口」。

| 对象类型 | 外部标识 | 来自哪个真值源 | 白盒为何漏掉 | 处置状态 | 认领文档 | 剩余风险 |
|---|---|---|---|---|---|---|
| 接口 | 〔示例：`POST /order/refund`〕 | 〔网关路由表〕 | 〔示例：路由由常量拼接，注解扫不到〕 | 〔已补查→进台账 / 待人工 / 列入残余风险〕 | 〔下单/api.md〕 | 〔示例：无〕 |
| 表 | 〔示例：`t_order_archive`〕 | 〔information_schema〕 | 〔示例：归档表，代码已无引用〕 | 〔列入残余风险〕 | 〔—〕 | 〔示例：疑似历史归档，重构需确认是否仍写入〕 |
| 〔填空〕 | 〔填空〕 | 〔填空〕 | 〔填空〕 | 〔填空〕 | 〔填空〕 | 〔填空〕 |

## 五、残余风险结论（给重构 AI 看的诚实陈述）

> 一句话说清「在什么边界内可信、什么地方可能漏」。**禁止**写「已查全/零遗漏/完整」。

〔填空，示例：在「注解枚举 + 网关路由表差集」边界内，HTTP/MQ/任务已逐行有归宿；
数据库层因未拿到 schema，整层为代码推断、低置信；反射式 RPC 为已知盲区，未覆盖。〕
