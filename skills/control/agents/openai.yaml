interface:
  display_name: "总控执行"
  short_description: "读取任务总控并推进指定单个子任务，严格回填状态且不越界"
  default_prompt: "使用 /control 推进 docs/00-任务总控/ 下的活跃总控的下一个可执行子任务。严格遵守执行边界：用户指定 Tn → 只动 Tn，做完立刻停止，不顺手做其他子任务。按子任务详情中的「强制阅读」「输入」「要做的事情」「不做什么」「输出物」「完成判定」执行，回填总控状态。"

policy:
  allow_implicit_invocation: false
