interface:
  display_name: "提交代码"
  short_description: "审查当前变更，过项目门禁后按项目提交规范精确 stage 并提交；仅明确要求时推送"
  default_prompt: "使用 /commit-changes 检查当前变更并按项目提交规范提交。"

policy:
  allow_implicit_invocation: false
