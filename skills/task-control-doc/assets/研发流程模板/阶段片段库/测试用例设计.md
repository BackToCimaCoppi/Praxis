# 阶段片段：测试用例设计（实例化为 `T{n}-测试用例设计.md`）

> 本文件是「测试用例设计」阶段的**片段模板**。实例化时替换 `{{槽位}}`、定编号、删本说明行。
> **评审默认塌缩进本片段末步**；命中死亡线/金标准领域才拆独立「评审」子任务。

## 当前状态

- **状态**：待完成
- **依赖**：T<施工蓝图>
- **阻塞**：T<施工>

## 子任务背景

测试用例设计阶段：从设计/蓝图产出**可自动化执行的测试用例规格**——能自动化的全部自动化（接口测试、端到端自动化、视觉验收交 VLM）。本阶段只**设计用例**，不写执行脚本（执行脚本归施工阶段）。

## 强制阅读（精准 1-3 个）

- `{{测试设计 skill}}`：测试用例规格真值 skill —— 读完获得用例分层标准与归位规则
- 上游 `_shared/T<施工蓝图>-施工蓝图.md`（验收映射）

## 输入

- 施工蓝图（验收映射）、轻量设计方案

## 要做的事情

1. 按 `{{测试设计 skill}}` 从验收映射推导测试用例（接口 / 端到端 / 视觉验收等）
2. 标明每条用例的自动化方式（能自动化的全自动化；只能手工验证的归手工验证清单）
3. **（评审塌缩时）** 跑评审，回补用例

## 不做什么

- 不写测试执行脚本（属施工阶段 `T<施工>`）
- 不改产品代码
- 不跑测试（设计阶段只产规格）

## 预期效果

测试用例规格成型，每条可追溯到验收映射，自动化方式明确，施工阶段可据此写执行脚本。

## 输出物

- `_shared/T{n}-测试用例.md`：用例规格（非 §1.1.1 标准名，普通专属资产）

## 完成判定

- [ ] 测试用例规格已产出，覆盖验收映射
- [ ] 每条用例标明自动化方式 / 手工归位
- [ ] 父级 `README.md` 子任务总表本行状态改为 `已完成`
- [ ] 父级 `README.md` 进展记录已追加一行

## 风险与注意事项

- 用例要终态断言、可复跑、遵守项目 ID/数据红线（见 `{{测试设计 skill}}`）
- 设计与执行分离：本阶段不落脚本

## 会话启动提示词（复制到新会话）

```
我要执行 docs/00-任务总控/{任务目录名}/T{n}-测试用例设计.md 这个子任务。

请按以下步骤：
1. 读取本文件 + 「强制阅读」列出的文件
2. 从验收映射设计测试用例规格，标自动化方式
3. 完成后回填本文件「当前状态」「输出物」，同步更新 README 子任务总表 + 进展记录
4. 不要做其他子任务，做完立刻停止并向我报告
```
