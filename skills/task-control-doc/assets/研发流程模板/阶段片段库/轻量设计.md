# 阶段片段：轻量设计（实例化为 `T{n}-轻量设计.md`）

> 本文件是「轻量设计」阶段的**片段模板**。实例化时替换 `{{槽位}}`、定编号、删本说明行。
> **评审默认塌缩进本片段末步**（产出→评审→升级用户决策→回补）；仅命中死亡线/跨域/契约破坏时，才把评审拆成独立的「评审」子任务（见 评审.md 片段）。

## 当前状态

- **状态**：待完成
- **依赖**：T<探讨>
- **阻塞**：T<蓝图>

## 子任务背景

轻量设计阶段：把探讨结论沉成**任务级设计**——用决策海拔锁死本次要改什么、怎么改的业务逻辑与关键技术取舍。它是用户审核层与施工期的局部临时真值。

## 强制阅读（精准 1-3 个）

- `{{轻量设计 skill}}`：轻量设计方法 —— 读完获得设计文档的结构与海拔要求
- 上游 `_shared/T<探讨>-探讨纪要.md`：已锁定的决策

## 输入

- 探讨纪要、调查报告

## 要做的事情

1. 按 `{{轻量设计 skill}}` 写轻量设计方案
2. **（评审塌缩时）** 跑一次自检/对抗评审，AI 拿不准的列「用户决策点」→ 用户裁决 → 回补方案
3. 提交用户审阅定稿

## 不做什么

- 不写逐文件变更清单（那是施工蓝图）
- 不碰代码
- 不替用户拍板关键取舍

## 预期效果

本次"改什么 / 怎么改"的业务逻辑与关键技术取舍锁死，用户已审阅，可据此出施工蓝图。

## 输出物

- `_shared/T{n}-轻量设计方案.md`（§1.1.1 标准名）
- （评审塌缩时）评审结论并入方案；如独立评审则见独立「评审」子任务

## 完成判定

- [ ] 轻量设计方案已产出并经用户审阅
- [ ] 用户决策点已逐条裁决并回补
- [ ] 父级 `README.md` 子任务总表本行状态改为 `已完成`
- [ ] 父级 `README.md` 进展记录已追加一行

## 风险与注意事项

- 命中死亡线区域（见项目补丁 `{{死亡线区域}}`）→ 不要塌缩评审，拆独立评审子任务并升级
- 设计海拔：锁死决策，不堆实现细节

## 会话启动提示词（复制到新会话）

```
我要执行 docs/00-任务总控/{任务目录名}/T{n}-轻量设计.md 这个子任务。

请按以下步骤：
1. 读取本文件 + 「强制阅读」列出的文件
2. 写轻量设计方案；拿不准的列用户决策点让我裁决；回补后我审阅
3. 完成后回填本文件「当前状态」「输出物」，同步更新 README 子任务总表 + 进展记录
4. 不要做其他子任务，做完立刻停止并向我报告
```
