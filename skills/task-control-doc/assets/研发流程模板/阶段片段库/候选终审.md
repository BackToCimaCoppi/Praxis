# 阶段片段：候选终审（实例化为 `T{n}-候选终审.md`）

> 本文件是「候选终审」阶段的**片段模板**。实例化时替换 `{{槽位}}`、定编号、删本说明行。
> **本阶段不可塌缩、不可省略**——goal 交付 `CANDIDATE_READY` 后无人终审是有实测事故记录的结构缺口（候选悬空无人验收，未合并分支在共享环境互相覆盖）。

## 当前状态

- **状态**：待完成
- **依赖**：T<goal 执行>
- **阻塞**：无

## 子任务背景

goal 已按章程自主跑完并交付精确候选（commit/tree SHA + 执行证据清单 + 飞行日志），状态 `CANDIDATE_READY`。本阶段是**流程终点的唯一独立验收**：对照冻结正式真值检查候选的实现、真实断言与执行证据——闸门全绿只证明报告结构与追溯完整，**语义正确性由本阶段兜底**。

## 强制阅读（核心必读 1-3 个，准入下限、非视野上限）

- `_shared/T<goal 执行>-交付记录.md` + 执行证据清单：候选 SHA、测试矩阵结果、豁免明细
- `_shared/T<goal 章程>-goal章程.md`：目标、已知豁免与交接面定义
- `_shared/T<规格冻结>-规格物化覆盖报告.md`：冻结锚点（`design_index_hash` / `spec_hash`）

## 输入

- 精确候选（commit/tree SHA）+ 执行证据清单 + 飞行日志
- 冻结的正式 L1–L7 与用例规格

## 要做的事情

1. 锁定候选 commit/tree SHA，确认工作树干净，复验规格清单与执行证据清单哈希
2. **打开真实测试落点**核对——不是只看 `AC-x` 字面出现：断言语义、强制测试形状（真并发/时钟边界/双路径/批量隔离）是否与冻结规格一致
3. 核对执行状态封闭集：只有 `VERIFIED` 计入通过；`BLOCKED / MISSING` 逐条核明细，`WAIVED` 逐条核正式来源或 `DEC-x`
4. 逐条审阅飞行日志的红→绿翻转与自愈记录，确认无语义改写混入
5. 核对**真实链路闭合度**：逐条过 `env_fidelity=simulated` 与 `BLOCKED` 清单，确认交付记录用人话写明真实链路断在哪、由谁何时接通
6. 高风险/死亡线候选：走 `{{评审 skill}}` 代码候选口径（同对象哈希、同真值基线只开放一次）；普通候选由主线程按本清单亲自执行
7. 意见逐条裁决；修复只针对已采纳固定清单，修复后走 `{{封闭验收 skill}}` 核对新候选 SHA，不重开发散评审
8. 若意见改变业务语义或暴露正式规格矛盾 → 退回冻结/人工裁决，不得就地改设计

## 不做什么

- 不重做施工、不扩张验收范围、不新增评审轮次
- 不把 `BLOCKED/MISSING` 折算进通过率；不接受无来源 `WAIVED`
- 不发明规则、不新增硬门

## 预期效果

候选获得**最终验收封印**（或带证据退回）；用户拿到一份可直接拍板的终审报告。

## 输出物

- `_shared/T{n}-候选终审报告.md`（§1.1.1 标准名）：候选 SHA / 逐项核对结果 / 裁决与整改闭环 / 最终封印或退回理由

## 完成判定

- [ ] 候选 SHA 锁定、工作树干净、哈希复验一致
- [ ] 全部 `AC-x` 真实落点核对完成；`VERIFIED` 之外的状态逐条有明细与来源
- [ ] 发现的意见全部裁决闭环；整改后封闭验收 `PASS`
- [ ] 最终验收封印已出具（存在 `BLOCKED/MISSING`、无来源 `WAIVED` 或未闭环发现时**禁止**出具）
- [ ] **此时才**把 T<goal 执行> 与本行状态改为 `已完成`；父级 `README.md` 进展记录已追加一行

## 会话启动提示词（复制到新会话）

```
我要执行 docs/00-任务总控/{任务目录名}/T{n}-候选终审.md 这个子任务。

请按以下步骤：
1. 读「强制阅读」三份文件，锁定候选 SHA 并复验哈希
2. 按「要做的事情」逐项核对真实测试落点与执行证据，不只看编号字面出现
3. 意见逐条裁决；需要我拍板的合并成一张表一次问我
4. 整改后走封闭验收，PASS 后出具最终验收封印
5. 回填本文件与 README（goal 执行子任务此时才可标已完成）
```
