# Tn - {子任务名}

> 本文件是一个**自包含工作包**：新会话读「本文件 + 强制阅读」即可开工——这是**准入下限，不是视野上限**。
> **鼓励**开工前主动补读：「背景导航」、父级 `README.md`、其他 `Tn-...md`、相关正式文档与代码现状，把背景挖够再动手。
> 唯一硬限制：自主补读**亲自读**（直接读文件/检索），不为补背景派子 agent / 起深度调查；**执行与写入仍严格限于本子任务**。

## 当前状态

- **状态**：待完成
- **依赖**：无 / T<n>
- **阻塞**：T<n+1>

## 子任务背景

（用一段话讲清楚为什么要做这件事，和总控的整体目标怎么挂钩）

## 强制阅读（核心必读 1-3 个，准入下限、非视野上限）

- `路径`：为什么必须读 + 读完获得什么结论
- `路径`：为什么必须读

## 背景导航（可选，不设上限）

- `路径`：读它获得什么背景（读不读由执行会话自行判断；**亲自读，不派 agent**）

## 输入

- 前置依赖的产出物：T<前置> 的 `输出文件路径`
- 用户提供的资料：

## 写入范围（机器检查）

- `start_commit`: `<开工前当前提交 SHA>`
- `allowed_write_paths`:
  - `本工作包路径`
  - `本子任务输出物路径或目录/**`
- `allowed_cross_task_writes`:
  - `README.md::子任务总表`
  - `README.md::进展记录`
- 交付检查：`python3 ~/.claude/skills/control/scripts/check_write_scope.py --repo <项目根> --start-commit <SHA> --candidate <本子任务提交SHA> --allow <路径> ... --allow-cross '<路径>::<Markdown标题>' ...`

## 要做的事情

1. 第一步
2. 第二步
3. 第三步

## 不做什么（建议填）

- 不做 X（属于 T<n+1>）
- 不修改 Y（属于其他模块）
- 不动 Z 文件

## 预期效果

（执行完后系统/文档应该是什么状态。和「输出物」的区别：输出物是文件清单，预期效果是行为/状态描述）

## 输出物

> **路径写法**：进入项目正式文档体系的写正式路径；过程资产（仅本任务有意义的中间文档）写 `_shared/T{n}-{资产名}`（本子任务专属）或 `_shared/{资产名}`（任务级共享，无前缀）。**所有资产都列在这里**，不另起「资产清单」段，不要平铺到任务根目录，不要新建 `_T{n}/` 目录。

- `具体路径/文件名`：内容简述
- `具体路径/文件名`：内容简述

## 完成判定

- [ ] 输出物 1 已产出
- [ ] 输出物 2 已产出且通过自检（如类型检查 / 编译 / 测试）
- [ ] 父级 `README.md` 子任务总表的本行状态已改为 `已完成`
- [ ] 父级 `README.md` 进展记录已追加一行

## 风险与注意事项

- 风险点 1
- 风险点 2

## 会话启动提示词（复制到新会话）

```
我要执行 docs/00-任务总控/{任务目录名}/Tn-{子任务名}.md 这个子任务。

【主体任务】{一句话：本子任务做什么、产出什么}
【目标终态】{完成判定的可度量压缩提要}
【边界提要】{「不做什么」关键禁令压缩；详细以本工作包为准，冲突时以工作包为准}

请按以下步骤：
1. 读取 docs/00-任务总控/{任务目录名}/Tn-{子任务名}.md（本文件）
2. 读取该文件「强制阅读」列出的文件；再主动补读「背景导航」和你自己判断需要的背景（父级 README、其他子任务、相关正式文档、代码现状），把背景挖够再动手
3. 自主补读必须亲自读（直接读文件/检索），不要为补背景派子 agent 或起深度调查
4. 严格在本子任务范围内执行：做完「要做的事情」、产出「输出物」、通过「完成判定」、遵守「不做什么」——读什么放开，做什么、写什么仍只限本子任务
5. 对本子任务候选提交运行写入范围检查器；越界不得交付
6. 完成后回填本文件的「当前状态」为已完成、「输出物」为实际产出文件
7. 同步更新 docs/00-任务总控/{任务目录名}/README.md 子任务总表对应行 + 进展记录
8. 不要做其他子任务，做完立刻停止并向我报告
```

> 子任务完成后，**直接回填**上面已有的字段（「当前状态」「输出物」「风险与注意事项」），并在父级 `README.md` 进展记录追加一行。**不需要**额外写"完成回填"段。
