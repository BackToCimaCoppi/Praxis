# Tn - {子任务名}

> 本文件是一个**自包含工作包**：新会话执行此子任务，**只读本文件 + 下面「强制阅读」列出的文件**。
> 不需要读其他 `Tn-...md` 或总控规范。
> 父级总控见同目录 `README.md`（执行时不必读，仅供查全局上下文）。

## 当前状态

- **状态**：待完成
- **依赖**：无 / T<n>
- **阻塞**：T<n+1>

## 子任务背景

（用一段话讲清楚为什么要做这件事，和总控的整体目标怎么挂钩）

## 强制阅读（精准 1-3 个）

- `路径`：为什么必须读 + 读完获得什么结论
- `路径`：为什么必须读

## 输入

- 前置依赖的产出物：T<前置> 的 `输出文件路径`
- 用户提供的资料：

## 要做的事情

1. 第一步
2. 第二步
3. 第三步

## 不做什么（建议填）

- 不做 X（属于 T<n+1>）
- 不修改 Y（属于其他模块）
- 不动 Z 文件

## 预期效果

（执行完后系统/文档应该是什么状态。和「输出物」的区别：输出物是文件清单，预期效果是行为/状态描述）

## 输出物

> **路径写法**：进入项目正式文档体系的写正式路径；过程资产（仅本任务有意义的中间文档）写 `_shared/T{n}-{资产名}`（本子任务专属）或 `_shared/{资产名}`（任务级共享，无前缀）。**所有资产都列在这里**，不另起「资产清单」段，不要平铺到任务根目录，不要新建 `_T{n}/` 目录。

- `具体路径/文件名`：内容简述
- `具体路径/文件名`：内容简述

## 完成判定

- [ ] 输出物 1 已产出
- [ ] 输出物 2 已产出且通过自检（如类型检查 / 编译 / 测试）
- [ ] 父级 `README.md` 子任务总表的本行状态已改为 `已完成`
- [ ] 父级 `README.md` 进展记录已追加一行

## 风险与注意事项

- 风险点 1
- 风险点 2

## 会话启动提示词（复制到新会话）

```
我要执行 docs/00-任务总控/{任务目录名}/Tn-{子任务名}.md 这个子任务。

请按以下步骤：
1. 读取 docs/00-任务总控/{任务目录名}/Tn-{子任务名}.md（本文件）
2. 读取该文件「强制阅读」列出的文件
3. 严格在本子任务范围内执行：做完「要做的事情」、产出「输出物」、通过「完成判定」、遵守「不做什么」
4. 完成后回填本文件的「当前状态」为已完成、「输出物」为实际产出文件
5. 同步更新 docs/00-任务总控/{任务目录名}/README.md 子任务总表对应行 + 进展记录
6. 不要做其他子任务，做完立刻停止并向我报告
```

> 子任务完成后，**直接回填**上面已有的字段（「当前状态」「输出物」「风险与注意事项」），并在父级 `README.md` 进展记录追加一行。**不需要**额外写"完成回填"段。
