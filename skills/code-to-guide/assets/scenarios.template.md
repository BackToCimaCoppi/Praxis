# 业务场景与数据流

> **理解层文档**：描述"端到端怎么跑"。设计动机见 [00-design.md](00-design.md)，接口详情见 [03-write-api.md](03-write-api.md)，文档导航见 [README.md](README.md)。

---

## 一、{{核心业务场景1}}

### 场景描述

{{用 1~2 句话描述这个场景的业务背景：谁做了什么，期望发生什么}}

### 触发条件

{{何时触发这个流程，入口在哪}}

### 数据流

```
调用方
  → {{模块A/接口名}}（入参：{{关键字段}}）
      → {{模块B/服务名}}（做了什么：{{关键逻辑}}）
          → {{数据库/外部服务}}（写入/读取：{{关键表/集合}}）
      ← 返回：{{关键返回字段}}
  ← 最终结果：{{业务结果}}
```

### 关键数据结构变化

| 阶段 | 对象/表 | 关键字段变化 |
|---|---|---|
| 进入{{模块A}} | {{实体名}} | `{{字段}}` = `{{值}}` |
| 进入{{模块B}} | {{实体名}} | `{{字段}}` 从 X 变为 Y |
| 写入持久层 | {{表名}} | 新增行 / 更新 `{{字段}}` |

### 注意事项

- {{边界条件、幂等性、事务范围等特殊说明}}

---

## 二、{{核心业务场景2}}

*(重复上方结构)*

---

## 三、{{核心业务场景3}}

*(重复上方结构)*

---

## 四、状态机（如适用）

*（如果项目有重要的状态流转，在此用表格或 ASCII 图描述）*

```
状态A ──[事件1]──→ 状态B ──[事件2]──→ 状态C
                              ↑
                         [事件3，来自状态A]
```

| 状态 | 含义 | 可触发的事件 |
|---|---|---|
| {{状态A}} | {{含义}} | {{事件1}}、{{事件3}} |
| {{状态B}} | {{含义}} | {{事件2}} |

---

## 五、典型操作对照表（可选）

| 操作 | 入口接口 | 核心步骤 | 影响的数据 |
|---|---|---|---|
| {{操作1}} | `{{接口路径}}` | {{简述}} | {{表/字段}} |
| {{操作2}} | ... | ... | ... |
