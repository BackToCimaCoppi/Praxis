# {{模块名}}

> **参考手册**：查 {{本文档覆盖内容，如"实体字段"/"接口清单"/"枚举值"}} 时使用。设计动机见 [00-design.md](00-design.md)，文档导航见 [README.md](README.md)。

---

## 一、核心实体

### {{实体名1}}（{{表名/集合名，如适用}}）

| 字段 | 类型 | 说明 |
|---|---|---|
| {{字段1}} | {{类型}} | {{含义，一句话}} |
| {{字段2}} | {{类型}} | {{含义}} |
| *（列出重要字段，不需要列所有字段）* | | |

**关键约定**：{{使用此实体时需注意的业务规则、唯一键、索引等}}

---

### {{实体名2}}

*(同上结构)*

---

## 二、数据关系

```
{{ASCII 关系图，例如：}}

EntityA
  ├── id (PK)
  └── entityBId → EntityB.id（N:1）

EntityB
  ├── id (PK)
  └── entityAList ← EntityA（1:N）
```

---

## 三、核心接口 / 服务方法（如适用）

| 方法 / 路径 | 功能 | 关键入参 | 关键返回 |
|---|---|---|---|
| `{{接口路径/方法名}}` | {{一句话功能}} | {{关键参数}} | {{关键返回字段}} |
| *（接口太多时，只列最常用的；完整列表见代码）* | | | |

---

## 四、关键枚举（如适用）

### {{枚举名}}

| 值 | 含义 |
|---|---|
| {{值1}} | {{含义}} |
| {{值2}} | {{含义}} |

---

## 五、设计要点（可选）

*（此模块有哪些非显而易见的约定、坑、或跨模块的耦合点，简要列出 2~5 条）*

- {{要点1}}
- {{要点2}}
