# 变更日志

本文件记录 Praxis 的重要变更，尤其是**破坏性变更**——如果你已经把 skill 拷进
`~/.claude/skills/`，升级前请先读一遍本文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [Unreleased]

### ⚠️ 行为变化

- 自主执行与轻量流程改为**角色化治理**：任务发起人、业务决策负责人、环境所有者/发布授权人、候选验收人分别声明；个人项目可由一人兼任，团队项目可按职责分离，当前交互方不再被默认视为全部权限持有人。
- 共享环境改为**项目策略驱动**：启动 goal 不构成环境授权或抢占授权；部署账本/租约的有效期、失效判据、可抢占角色与冲突处置由项目级补丁声明。缺少释放、失效或明确抢占授权时，优先使用隔离环境，否则交付可恢复的 `EXECUTION_BLOCKED`。
- 人工协作统一改为写明责任角色的「人工里程碑」，不再假设由同一个用户完成，也不再设置通用的 0/1 次窗口目标。

### 🔒 脱敏

- 变更日志不再复述已删除的项目标识符；现行文本与贡献规范增加治理角色和共享环境的通用化检查。

## [2.1.0] — 2026-08-23

这一版把「结果管控」闭环从纸面收紧到可机检：写入范围机器核对、证据链单向不可自证、
启动检查并入 goal、评审者强制独立勘察。skill 数量 23 → 25，多个 skill 的运行语义有变化，
升级前请对照下表。

### ⚠️ 行为变化（会改变既有流程的判定结果）

| 变更 | 影响 | 迁移建议 |
|---|---|---|
| **`goal-charter` 停机白名单 4 条 → 3 条** | 「凭据找不到」「缺陷修复无唯一低风险答案」「同一失败点连续三次」不再是停机理由，统归 goal 内工程问题 | 章程只保留：两份冻结真值矛盾且影响业务结果 / 死亡线业务规则要变 / 未预授权的不可逆或外发动作；限轮或安全路径穷尽改为交付 `EXECUTION_BLOCKED` 断点 |
| **章程红队由「每次必打」→「按风险触发或用户点选」** | 普通任务不再自动跑一轮对抗评审 | 命中死亡线、跨域/破坏契约或用户显式要求时才开；`task-control-doc` 标准研发流程 ⑥ 同步 |
| **`test-execution-router` 失败分类改名/增类** | `TEST_BUG` 改为 `HARNESS_BUG`；新增 `AUTHORITY_REQUIRED`；`SPEC_STALE` 取消「goal 态走回炉」特判，改按语义影响面回用例层或设计层 | 更新项目执行 skill 的分类映射；`MANUAL_REQUIRED` 现在必须附物理边界证明 |
| **`adversarial-review` 报告结构 8 段 → 10 段** | 新增「评审者调查账与通道信息缺口」「参与模型 + 通道点评与任务内排名」，其余段号顺延 | 存量报告不必改；新评审按新模板出 |
| **`adversarial-review` 默认评审者 A 由 Opus 5 改为 Fable 5** | 默认阵容 = 原生 Fable 5 + codex GPT-5.6-Sol max；Cursor Grok 4.6 Extra High / GLM 5.2 Max / Kimi K3 Max 为可选评审者，只在用户明确点选时加入；全员禁 Fast | 不需要 Cursor 的用户无影响；要用可选评审者需安装 Cursor CLI 并登录，走 `scripts/cursor-review-runner.sh` |
| **测试/环境就绪不再是章程准入门票** | 环境、测试资产、runner、夹具、证据工具改由 goal 首个里程碑 **M0 启动检查** 自行建立/修复 | 删除项目里独立的「执行准备」阶段；章程准入只看规格冻结 + 物化覆盖报告全绿 + 真实动作授权 |

### ✨ 新增（2 个 skill）

- `plan-goal` — 轻量任务流程：总控/标准研发流程的微缩档。一份计划文件（九节：终态 / 开场声明 / 事实基线 / 决策点 / 改动 / 测试 AC 表 / 文档同步 / 执行章程 / 执行记录）→ 授权批准 = 决策生效 + 冻结 → 转 goal 跑到底 → 指定角色终点验收。三档分级（直接做 / plan-goal / 升级）与升级触发写死，防止小任务硬套重流程、也防止跳过流程后决策没留痕。
- `one-by-one` — 逐题裁决模式：仅用户手动触发；禁弹窗，纯文本一题一讲（背景 / 分歧点 / 选项含代价 / 推荐）、一裁一落盘、记完再下一题；假决策三分法（上游决策点不吞、自造题讲不出差异不出、答不出差异即补背景）。

### ✨ 新增机制

**评审**

- `adversarial-review`：可选评审者 C/D/E（Cursor Grok / GLM / Kimi）按用户逐个点选加入，最多五名；评审者 A 需钉死 xhigh 时提供 `claude -p --effort xhigh` 只读 CLI 路线。评审者必须在授权项目根 `REVIEW_ROOT` 内**强制独立只读勘察**并先交调查账（缺勘察重试一次、仍缺判该方失败）；证据包冻结**读取禁区**（本地环境文件、私钥、凭据存储、主线程裁决草稿）与**动作禁区**（执行/联网/MCP/派子 agent），触碰即判失败；通道读不到登记 `INFO-GAP`，不得评成模型漏报；裁决完成后主线程对全部参与模型做**不并列的任务内排名**（先裁决后评分，不反向改裁决）。

**总控三驾马车**

- `task-control-doc` / `control`：工作包新增**写入范围**三字段（`start_commit` / `allowed_write_paths` / `allowed_cross_task_writes`，跨任务写入精确到 Markdown 标题）；交付前用新脚本 `control/scripts/check_write_scope.py` 对候选提交做机器核对，越界即失败，无关并发提交不阻断，禁止事后扩白名单变绿。
- `control`：长程 goal **磁盘复水**——首次启动 / `--resume` / 新会话 / 上下文压缩后，第一次写入前必须重读工作包、章程、断点文件 `_shared/T{n}-goal断点.json` 与飞行日志尾部；操作性阻塞可交出 `EXECUTION_BLOCKED` 但不取得业务停机权。
- `task-control-doc`：标准研发流程 ⑥ 新增 **goal 启动服务单**（把可预见的用户动作按 `AI_AUTO / PRE_GOAL / CANDIDATE_DEPENDENT / LONG_LATENCY_EXTERNAL` 提前清算，是现场基线不是准入闸）；⑧ 候选终审改为**全新上下文只读**，缺陷按唯一路由退回，不在终审里顺手修。

**自主执行**

- `goal-charter`：新增 M0 启动检查与**测试基础设施自愈**；执行证据改为 **raw → 采集时不可变信封 → AC 映射 → 账本** 的单向链，`VERIFIED` 必须可重算，禁止账本自证 / 反向改 raw / 事后补绑候选；新增**执行资产交接**（任务临时 / 领域复用 / 项目通用三分类，可复用项晋升并提交）；允许段改为显式继承上游授权（环境 / 动作 / 对象 / 费用含重试储备 / 副作用）且不得扩大。
- `deep-research-gate`：补齐 `SessionStart` 自检钩子的兜底方案与覆盖边界（只补救下一会话，当前会话内仍靠纪律）。

**测试三件套**

- `test-standards`：手工验证只留给**物理边界**（AI 可控浏览器 / DOM / 截图 + VLM 判读一律算自动化）；测试矩阵固定 **8 个执行面每面一行**，不命中须写可核验 N/A 理由；运行痕迹（退出码 / 报告哈希 / 测试名）不能冒充业务断言证据，新增三条对应阻断条件。
- `test-case-design`：每条 AC 新增 `result` **证明部件**（证据必须绑定框架原生断言事件 / 真实扫描 / 物理边界原始观测），高风险 AC 须附证明部件覆盖表；金标准硬要求 4 条 → 5 条；新增 `manual_runbook` 字段；冻结后失败按职责路由（产品 / 测试资产 / 用例语义 / 规格）。
- `test-execution-router`：同一 goal 内拆为 **`bootstrap`**（M0，每面一个健康探针，禁止逐 AC 签 Ready）与 **`verification`** 两种模式；新增 §3.5 单向证据协议（`evidence_semantics_version: assertion-native-v1`、消费者完整性、候选前观测分布报告）；可复用执行资产三类晋升与 `PROPAGATION_REQUIRED` 交接语义。

**其他**

- `doc-layer-system` / `lightweight-design`：分档表与 goal 执行态例外补入「轻量任务流程」档位（对应 `plan-goal` skill，本版尚未收录）。
- `ask-codex` / `codex-review` / `doc-html-style` / `design-preview`：description 收敛为「仅手动触发」口径，正文不变。
- `commit-changes`：由项目级升格为用户级通用引擎——提交规范原文 / 门禁命令 / 额外首行规则改为「挂载点按序探测」，不再硬编码任何项目值；description 收敛为「仅手动触发」。
- 新增随包单元测试：`control/tests/`（写入范围校验、启动提示词复水句）、`task-control-doc/tests/`（八阶段顺序、M0 替代执行准备、终审只读路由）、`test-execution-router/tests/`（跨 skill 措辞契约）。
- 停机后处置改革（`goal-charter` / `task-control-doc` / `test-case-design` / `test-execution-router` / `codex-review` / `plan-goal`）：停机或终审发现上游语义缺陷时，**默认热修**——随附修改方案（决策项 + 上游修改清单 + 失效证据清单 + 恢复点 + 档位建议），由相应授权角色按项目治理批准后，改齐设计/规格/用例并断点续跑同一 goal；回炉降为需要业务决策负责人批准的例外档。
- `goal-charter`：新增「上游资产完整性错位」自愈行（断链/哈希失配/编号错位按真值优先级修复留痕继续，不停机）。环境占用语义已在 `[Unreleased]` 更正为项目策略驱动；停机白名单在 `control` / `plan-goal` / `test-execution-router` / 流程模板中的复抄改为「决策完整一行提要 + 指针」，真值收敛到 `goal-charter` §4 与 `control` 总控规范 §4.5 两处。
- `long-doc-governance`：文档长度扫描脚本 `scripts/doc-length-check.sh` 与默认阈值配置随 skill 附带，不再指向仓外路径；项目可用 `.claude/doc-length-config.json` 覆盖。
- 一致性：`construction-blueprint` 明确为「传统人工逐工序流程可选」，正文中「施工蓝图已退役」的口径统一为「本流程不再产出」；修正 `ask-codex` / `codex-review` / 三张评审口径卡对对抗评审旧编制（Opus 裁判、`§7.2`）的陈旧引用；`docs/lightweight-design.md`、`docs/git-worktree.md` 重新对齐正文；贡献规范自查新增第 ④ 条（家目录写法路径）。
- 文档：16 篇 `docs/<skill>.md` 与新正文重新对齐，新增 `docs/plan-goal.md`、`docs/one-by-one.md`；README 修正 `adversarial-review` 的依赖说明（没有 codex 不能降级为单侧评审）。

### 🔒 脱敏

- 全库逐字复审（机械扫描 + 逐文件通读），清掉机械规则抓不到的叙事性残留：真实事故的数字、日期和引文改为问题空间表述；项目特定的任务编号、业务命名、字段名、错误码和目录约定统一换成虚构示例；对项目特定规则与裁决记录的引用改为自包含表述。变更日志只记录脱敏类别，不复述被删除的原值。

---

## [2.0.0] — 2026-07-28

这一版把整个库从「过程管控」重心转向「**结果管控**」：人不再逐道工序把关，而是在
**起点拍板契约、终点验收候选**，中间交给可判定的验证器与边界。skill 数量 16 → 23。

### ⚠️ 破坏性变更

| 变更 | 影响 | 迁移建议 |
|---|---|---|
| **删除 `ask-claude` / `ask-opus` / `ask-sonnet`** | 这三个 skill 不再提供 | 日常派活直接用 `Agent` 工具；需要外部模型第二意见改用 `ask-codex`；证据包方法论已内置进 `adversarial-review/references/证据包制作规范.md`，不再依赖它们的 dispatch |
| **`adversarial-review` 取消独立裁判轮** | 流程从「双评审 + 第三个裁判线程」变为「双评审 + **主线程直接裁决**」 | 无需改配置。主线程本就持有全部上下文与真值基线，另起裁判线程反而让上下文越传越薄 |
| **`adversarial-review` 同一对象只允许评审一次** | 不能再对同一对象跑 R2/R3 | 整改后改用 `closed-remediation-review` 做封闭验收。想重开必须先登记旧评审失效原因与新哈希 |
| **`lightweight-design` 语义反转：事后回补 → 事前物化** | 轻量设计**不再是施工期的第二套真值** | 原流程「施工期以设计文档为准、完工后回补七层」已废弃。改为冻结前把全部有效语义物化进正式七层，轻量设计只保留"为什么这么选" |
| **`task-control-doc` 研发流程八阶段重排** | 阶段片段库删除「施工蓝图 / 文档闸门 / 文档收尾 / 交付发布」，新增「规格冻结 / goal 章程 / goal 执行 / 候选终审」 | 存量任务不受影响（旧片段只是不再随新任务生成）。`construction-blueprint` skill **本身保留**，仍按人逐工序把关的流程可继续使用 |
| **`test-standards` 移除「旧引用兼容索引」** | 该节映射的是某个项目的历史质量体系章节号 | 对其他项目本就无意义，直接按新三件套使用 |

### ✨ 新增（10 个 skill）

**自主执行**

- `goal-charter` — 把施工压成「目标 + 验证器 + 边界 + 自愈清单」的执行契约，保留起点批准与终点验收两个治理检查点。含契约 →「让它自己跑」开关的交接配方。
- `deep-research-gate` — 扇出安全门：有界并行直接放行，深度研究与递归工作流引擎级焊死。

**测试三件套**

- `test-standards` — 测什么、测多深、什么不许交付。
- `test-case-design` — 施工前冻结的用例规格；断言有稳定编号、哈希锚在规格而非脚本。
- `test-execution-router` — 冻结用例路由到项目执行层，编号逐条对账，失败强制分类。

**评审闭环**

- `closed-remediation-review` — 整改验收，验收清单冻结后不得扩张（往回拧评审棘轮的那颗螺丝）。
- `codex-review` — 施工后的低成本符合性核验，默认方向「不改除非已证实是缺陷」。
- `ask-codex` — 轻量调用外部模型问问题或派任务。

**呈现 / 可视化**

- `design-preview` — UI 形态还原成像素级 HTML 并弹进浏览器，对着真图评审。
- `doc-html-style` — 桌面优先、色彩克制而丰富的自包含文档 HTML。

### 🔧 改进

- **`control`**：读不设禁区（强制阅读是准入下限、不是视野上限），但补读必须亲自读、禁止为补背景派子 agent；规则铸造权收归项目指令与 skill；停机白名单收窄到三条，其余先修后报；建立总控即预授权提交。
- **`doc-layer-system`**：新增「真值不得下放」与「业务语义来源可审计」两条核心机制；冻结前增加跨层可满足性与业务主题唯一性检查；为自主执行态开明确例外。
- **`code-to-7layer` / `docs-from-code`**：代码只产候选规格，**不取得裁决权**；候选底稿与正式规格分离；冲突并列登记，禁止自动选边。
- **`git-worktree`**：从硬编码 `master` 改为运行时探测主干分支名与目录布局，不再绑定特定命名。
- **`long-doc-governance`**：文档类型举例并列两代流程。
- **文档**：每个 skill 的四段式说明文档全部与 skill 正文重新对齐；README 新增「自主执行 / 测试 / 呈现可视化」三个分类与结果管控闭环入口。

### 🔒 安全 / 脱敏

- 清除历史中混入的私有项目源码标识符，示例统一改用通用订单域。
- 脱敏自检规则改为「只拦真实泄漏模式」：不再把 `token` / `密钥` / `secret` 当字面敏感词
  （这些在技术文档里是高频普通名词，字面拦截会让自检每次命中一堆误报，人一旦习惯性忽略，
  这道检查就等于不存在），改为只拦「词后面跟着真实值」的赋值模式与各家密钥前缀。

---

## [1.0.0] — 2026-06

首个公开版本，16 个 skill：七层文档治理、对抗评审、任务总控三驾马车，
外加冷启动反推、老代码考古、施工蓝图、派活问询与 Git 流程。
